services:
  - type: web
    name: n8n-ai-platform
    env: docker
    plan: free
    region: frankfurt
    envVars:
      - key: N8N_SECURE_COOKIE
        value: "false"
      - key: NODE_ENV
        value: production
